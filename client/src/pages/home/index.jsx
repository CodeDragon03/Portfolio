import { motion } from 'framer-motion';

const Home = () => {
    return (
        <>
            <title> Protfolio | Harbour</title>
            <motion.div className="home"></motion.div>
        </>
    );
};

export default Home;
